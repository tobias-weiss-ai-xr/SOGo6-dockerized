[store."default"]
type = "postgresql"
host = "sogo6-postgres"
port = 5432
database = "stalwart"
auth-username = "stalwart"
auth-secret = { type = "value", value = "stalwart_sogo6_2026" }

[directory."ldap"]
type = "ldap"
addresses = ["ldap://sogo6-ldap:389"]
bind-dn = "cn=admin,dc=example,dc=org"
bind-password = "admin"
base-dn = "ou=users,dc=example,dc=org"
filter = "(objectClass=inetOrgPerson)"
